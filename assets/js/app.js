document.addEventListener('DOMContentLoaded',async()=>{const s=await fetch('/data/state_data.json').then(r=>r.json());const sel=document.getElementById('state');if(sel){sel.innerHTML=Object.keys(s).map(k=>`<option value="${k}">${k.replace(/-/g,' ')}</option>`).join('');} });